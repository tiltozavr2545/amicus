# macOS
.DS_Store

# Secrets
.env
.env.*
# ...кроме шаблонов: они и заводятся для того, чтобы лежать в репозитории.
# Без этой строки `console/.env.example` молча не коммитился, хотя README
# велит с него копировать (`app/.env.example` уцелел только потому, что его
# добавили до появления правила выше).
!.env.example
.serena

# Various
*.pdf

# Local scratch/backup files, not part of the repo
archive/
