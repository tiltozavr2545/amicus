gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Firebase config for this build — project-specific, not a secret per se
# (it's bundled into the shipped APK either way), but kept out of git the same
# way key.properties is: no reason to commit a file whose only source of truth
# is the Firebase console.
app/google-services.json
