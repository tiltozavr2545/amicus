<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Debug/profile manifests declare this too (Flutter tool needs it for
         hot reload etc.), but release doesn't inherit from those — without
         it here, the release build has no network access at all. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Android 13+ (API 33) requires this to be declared and granted at
         runtime before a notification will actually show; requested via
         FirebaseMessaging.instance.requestNotificationsPermission() in
         push_notifications.dart. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Auto Backup is on by default, and it sweeps up SharedPreferences —
         which is exactly where supabase_flutter keeps the session (access and
         refresh tokens, in plain XML) and where FeedCache keeps cached posts
         and signed media URLs. Backed up to the user's Drive and restored on
         another device, that refresh token is a working session for the
         account without the password ever being involved.

         allowBackup="false" closes cloud backup on every version; the two
         rule files below close device-to-device transfer on Android 12+,
         which allowBackup alone does not. -->
    <application
        android:label="Amicus"
        android:name="${applicationName}"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
